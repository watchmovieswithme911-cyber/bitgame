// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  GameType,
  GameCreateInput,
  GameUpdateInput,
  GamePurchaseType,
  GamePurchaseCreateInput,
  GamePurchaseUpdateInput,
  CommissionLedgerType,
  CommissionLedgerCreateInput,
  CommissionLedgerUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Game Client Functions
// ============================================================================

/**
 * List all Game records
 */
export async function getGameList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GameType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/games${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Game')
  }
  const json = await response.json()
  return (json.items || []) as GameType[]
}

/**
 * Get a single Game by ID
 */
export async function getGameById(args: { data: { id: string; userId?: string } }): Promise<GameType> {
  const url = `${getApiBase()}/api/games/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Game not found')
  }
  const json = await response.json()
  return json.data as GameType
}

/**
 * Create a new Game
 */
export async function createGame(args: { data: { input: GameCreateInput; userId?: string } }): Promise<GameType> {
  const url = `${getApiBase()}/api/games`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Game')
  }
  const json = await response.json()
  return json.data as GameType
}

/**
 * Update an existing Game
 */
export async function updateGame(args: { data: { id: string; input: GameUpdateInput; userId?: string } }): Promise<GameType> {
  const url = `${getApiBase()}/api/games/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Game')
  }
  const json = await response.json()
  return json.data as GameType
}

/**
 * Delete a Game
 */
export async function deleteGame(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/games/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Game')
  }
  return { success: true }
}

// ============================================================================
// GamePurchase Client Functions
// ============================================================================

/**
 * List all GamePurchase records
 */
export async function getGamePurchaseList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GamePurchaseType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/game-purchases${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list GamePurchase')
  }
  const json = await response.json()
  return (json.items || []) as GamePurchaseType[]
}

/**
 * Get a single GamePurchase by ID
 */
export async function getGamePurchaseById(args: { data: { id: string; userId?: string } }): Promise<GamePurchaseType> {
  const url = `${getApiBase()}/api/game-purchases/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'GamePurchase not found')
  }
  const json = await response.json()
  return json.data as GamePurchaseType
}

/**
 * Create a new GamePurchase
 */
export async function createGamePurchase(args: { data: { input: GamePurchaseCreateInput; userId?: string } }): Promise<GamePurchaseType> {
  const url = `${getApiBase()}/api/game-purchases`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create GamePurchase')
  }
  const json = await response.json()
  return json.data as GamePurchaseType
}

/**
 * Update an existing GamePurchase
 */
export async function updateGamePurchase(args: { data: { id: string; input: GamePurchaseUpdateInput; userId?: string } }): Promise<GamePurchaseType> {
  const url = `${getApiBase()}/api/game-purchases/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update GamePurchase')
  }
  const json = await response.json()
  return json.data as GamePurchaseType
}

/**
 * Delete a GamePurchase
 */
export async function deleteGamePurchase(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/game-purchases/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete GamePurchase')
  }
  return { success: true }
}

// ============================================================================
// CommissionLedger Client Functions
// ============================================================================

/**
 * List all CommissionLedger records
 */
export async function getCommissionLedgerList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CommissionLedgerType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/commission-ledgers${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list CommissionLedger')
  }
  const json = await response.json()
  return (json.items || []) as CommissionLedgerType[]
}

/**
 * Get a single CommissionLedger by ID
 */
export async function getCommissionLedgerById(args: { data: { id: string; userId?: string } }): Promise<CommissionLedgerType> {
  const url = `${getApiBase()}/api/commission-ledgers/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'CommissionLedger not found')
  }
  const json = await response.json()
  return json.data as CommissionLedgerType
}

/**
 * Create a new CommissionLedger
 */
export async function createCommissionLedger(args: { data: { input: CommissionLedgerCreateInput; userId?: string } }): Promise<CommissionLedgerType> {
  const url = `${getApiBase()}/api/commission-ledgers`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create CommissionLedger')
  }
  const json = await response.json()
  return json.data as CommissionLedgerType
}

/**
 * Update an existing CommissionLedger
 */
export async function updateCommissionLedger(args: { data: { id: string; input: CommissionLedgerUpdateInput; userId?: string } }): Promise<CommissionLedgerType> {
  const url = `${getApiBase()}/api/commission-ledgers/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update CommissionLedger')
  }
  const json = await response.json()
  return json.data as CommissionLedgerType
}

/**
 * Delete a CommissionLedger
 */
export async function deleteCommissionLedger(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/commission-ledgers/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete CommissionLedger')
  }
  return { success: true }
}
